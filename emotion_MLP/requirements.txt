# ==================== Web 服务 ====================
# Flask：数据采集服务器（training/pc_vision_server.py）
flask>=3.0.0

# ==================== 核心依赖 ====================
torch>=1.10.0,<=2.3.1
numpy>=1.21.0,<=1.26.4
scikit-learn>=1.0.0
opencv-python>=4.8.0
mediapipe>=0.10.0
tqdm>=4.64.0
pandas>=1.5.0
joblib>=1.3.0

# ==================== 语音 / TTS ====================
# edge-tts: 微软Edge TTS，用于文字转语音（无需API Key）
edge-tts>=6.1.0
# dashscope: 阿里云 DashScope TTS（Sambert语音合成，需要API Key）
dashscope>=1.14.0
# pyaudio: 音频采集（Windows 下建议用 pipwin 安装，见下方注释）
pyaudio>=0.2.11
# g2p-en: 英文音素解析（test_speech_lower.py 中英双语支持）
g2p-en>=2.0.0
# pypinyin: 中文拼音转换（test_speech_lower.py）
pypinyin>=0.49.0
# sounddevice: 音频播放/录制（跨平台替代方案）
sounddevice>=0.4.6
# scipy: 音频信号处理（滤波、互相关、谱减法）
scipy>=1.10.0

# ==================== 大模型 / API ====================
# openai: DeepSeek API 调用（兼容 OpenAI SDK）
openai>=1.0.0
# websocket-client: 讯飞语音识别 WebSocket 连接
websocket-client>=1.6.0

# ==================== 音频播放 ====================
pygame>=2.5.0

# ==================== 本地 ASR / SenseVoice ====================
funasr>=1.0.0
modelscope>=1.14.0
# rotary_embedding_torch (SenseVoice 依赖)
rotary_embedding_torch>=0.5.3

# ==================== 视觉（vision/ 子系统） ====================
# face-recognition + dlib: GPU人脸识别
# 注意：dlib 在 Windows 上建议通过 .whl 安装，或使用 conda
face-recognition>=1.3.0
dlib>=19.24.0

# ==================== 安装建议 ====================
# 1. PyTorch GPU 版：根据你的 CUDA 版本选择
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# 2. pyaudio 在 Windows 上如果安装失败：
#    pip install pipwin && pipwin install pyaudio
#    或从 https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio 下载 .whl
#
# 3. dlib 在 Windows 上如果安装失败：
#    从 https://github.com/z-mahmud22/Dlib_Windows_Python3 下载对应 Python 版本的 .whl
#
# 4. 一次性安装全部依赖：
#    pip install -r requirements.txt
